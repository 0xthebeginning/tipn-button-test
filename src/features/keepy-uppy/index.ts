/**
 * Public surface of the keepy-uppy feature. The app shell should import
 * only from here — keeping the boundary clean so the feature can be
 * lifted into its own miniapp (or a /games/keepy-uppy route) later.
 */
export { KeepyUppyGame } from './components/KeepyUppyGame';
