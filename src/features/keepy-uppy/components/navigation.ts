/**
 * Where "Back to Stickers" goes. Isolated so the route can change in one
 * place if the sticker app lives somewhere other than the miniapp root —
 * or if this game later moves to /games/keepy-uppy in its own miniapp.
 */
export const BACK_TO_STICKERS_HREF = '/';
